# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more

# SUPER key
$mainMod = SUPER

# Applications
bind = $mainMod,T, exec, kitty # Open Kitty terminal
bind = $mainMod, A, exec, ~/.config/scripts/open_web_app.sh https://gemini.google.com/ # Open Gemini web app
bind = $mainMod, Q, killactive # Close active window
bind = $mainMod, E, exec, thunar # Open Thunar file manager
bind = $mainMod SHIFT, A, exec, pavucontrol # Open PulseAudio volume control
bind = $mainMod, B, exec, google-chrome-stable # Open Google Chrome browser
bind = $mainMod, G, exec, ~/.config/scripts/open_web_app.sh https://mail.google.com/ # Open Gmail web app
bind = $mainMod SHIFT, B, exec, blueman-manager # Open Blueman Bluetooth manager

# Rofi
bindr = SUPER, SUPER_L, exec, ~/.config/scripts/toggle-launcher.sh # Toggle Rofi and Waybar together
bind = $mainMod, I, exec,~/.config/rofi/wifi-menu.sh 
bind = $mainMod SHIFT, I, exec, rofi -show window # Show all open windows in Rofi

# Navigation (Windows)
bind = $mainMod, Tab, cyclenext, # focus next window 
bind = $mainMod SHIFT, Tab, cyclenext, prev # focus previous window
bind = $mainMod ALT, Tab, layoutmsg, swapnext
bind = $mainMod SHIFT ALT, Tab, layoutmsg, swapprev

# Navigation (Workspace)
bind = $mainMod, W, workspace, r+1 # focus next workspace
bind = $mainMod SHIFT, W, workspace, r-1 # focus previous workspace
bind = $mainMod ALT,W, movetoworkspace, r+1 # move window to next workspace
bind = $mainMod SHIFT ALT,W, movetoworkspace, r-1 # move window to previous workspace
bind = $mainMod, D, focusmonitor, +1 # focus next display
bind = $mainMod, mouse_down, workspace, e+1 # Scroll workspaces 
bind = $mainMod, mouse_up, workspace, e-1 # Scroll workspaces


bind = $mainMod, 1, workspace, 1 # Switch to workspace 1
bind = $mainMod, 2, workspace, 2 # Switch to workspace 2
bind = $mainMod, 3, workspace, 3 # Switch to workspace 3
bind = $mainMod, 4, workspace, 4 # Switch to workspace 4
bind = $mainMod, 5, workspace, 5 # Switch to workspace 5
bind = $mainMod, 6, workspace, 6 # Switch to workspace 6
bind = $mainMod, 7, workspace, 7 # Switch to workspace 7
bind = $mainMod, 8, workspace, 8 # Switch to workspace 8
bind = $mainMod, 9, workspace, 9 # Switch to workspace 9
bind = $mainMod, 0, workspace, 10 # Switch to workspace 10

bind = $mainMod SHIFT, 1, movetoworkspace, 1 #  Move window to workspace 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2 #  Move window to workspace 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3 #  Move window to workspace 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4 #  Move window to workspace 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5 #  Move window to workspace 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6 #  Move window to workspace 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7 #  Move window to workspace 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8 #  Move window to workspace 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9 #  Move window to workspace 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10 #  Move window to workspace 10


# Navigation (Display)
bind = $mainMod SHIFT, D, focusmonitor, -1 # focus next display
bind = $mainMod ALT, D, movewindow, mon:+1 # move window to next display
bind = $mainMod SHIFT ALT, D, movewindow, mon:-1 # move window to next display
bind = $mainMod CONTROL, D, movecurrentworkspacetomonitor, +1 # Move workspace to next monitor
bind = $mainMod SHIFT CONTROL, D, movecurrentworkspacetomonitor, -1 # Move workspace to next monitor

# Window Sizes
bind = $mainMod, M, exec, ~/.config/scripts/master_toggle.py # toggles current window with master
bind = $mainMod ALT, M, fullscreen, 0 # Toggle fullscreen
bind = $mainMod SHIFT, M, fullscreen, 1 # Maximize window

bind = $mainMod, left, resizeactive, -10 0 # Resize window left
bind = $mainMod, right, resizeactive, 10 0 # Resize window right
bind = $mainMod, up, resizeactive, 0 -10 # Resize window up
bind = $mainMod, down, resizeactive, 0 10 # Resize window down

bind = $mainMod SHIFT, left, resizeactive, -100 0 # Resize window left large
bind = $mainMod SHIFT, right, resizeactive, 100 0 # Resize window right large
bind = $mainMod SHIFT, up, resizeactive, 0 -100 # Resize window up large
bind = $mainMod SHIFT, down, resizeactive, 0 100 # Resize window down large

# Actions
bind = $mainMod, escape, exec, hyprlock # Lock screen
bind = $mainMod, K, exec, ~/.config/scripts/keybindings.sh # Open Keybinds menu
bind = $mainMod, P, exec,wlogout -b 4 #-s # Show logout menu
bind = $mainMod, S, exec, ~/.config/scripts/screenshot.sh --region # Take a region screenshot
bind = $mainMod SHIFT, S, exec, ~/.config/scripts/screenshot.sh --fullscreen # Take a fullscreen screenshot
bind = $mainMod, Print, exec, ~/.config/scripts/screenshot.sh --window # Take a screenshot of the active window
bind = , XF86AudioRaiseVolume, exec, wpctl set-volume -l 1.4 @DEFAULT_AUDIO_SINK@ 5%+ # Increase volume
bind = , XF86AudioLowerVolume, exec, wpctl set-volume -l 1.4 @DEFAULT_AUDIO_SINK@ 5%- # Decrease volume
bind = , XF86MonBrightnessUp, exec, brightnessctl set 10%+ # Increase screen brightness
bind = , XF86MonBrightnessDown, exec, brightnessctl set 10%- # Decrease screen brightness
bind = , XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle # Mute/unmute audio
bind = , XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle # Mute/unmute microphone
bind = , XF86WLAN, exec, nmcli radio wifi toggle # Toggle Wi-Fi
bind = , XF86Refresh, exec, xdotool key F5 # Refresh active window (sends F5)

# Triggered when the lid is closed
bindl=,switch:on:Lid Switch, exec, loginctl lock-session

# Triggered when the lid is opened
bindl=,switch:off:Lid Switch, exec, hyprctl dispatch dpms on
